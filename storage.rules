rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to the uploads folder
    match /uploads/{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
